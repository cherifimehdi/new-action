name: First Workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps: 
      - name: Print greeting
        run: echo "HELLO FIRST ACTIONS"
      - name: Print By
        run: echo "FINE....BYE"
